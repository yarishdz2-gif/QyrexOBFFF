{
  "name": "qyrex-obf",
  "version": "3.0.0",
  "description": "QyrexOBF PureNode v3 — same files, 100% Node.js (no lua binaries)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node -e \"const {obfuscate}=require('./obfuscate'); const r=obfuscate('print(1)',{mode:'max'}); console.log(r.steps.join(' → '));\""
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
